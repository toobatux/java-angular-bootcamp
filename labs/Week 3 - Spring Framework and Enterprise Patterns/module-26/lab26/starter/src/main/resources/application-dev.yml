spring:
  datasource:
    # TODO: confirm H2 in-memory URL for lab (must match solution contract)
    url: jdbc:h2:mem:lab26dev;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
logging:
  level:
    com.northstar.crm: DEBUG

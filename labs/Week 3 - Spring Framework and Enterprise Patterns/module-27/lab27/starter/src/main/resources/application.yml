spring:
  application:
    name: northstar-crm
  datasource:
    url: jdbc:h2:mem:lab27;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080

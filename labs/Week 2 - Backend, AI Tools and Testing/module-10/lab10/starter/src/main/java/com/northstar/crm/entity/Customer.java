package com.northstar.crm.entity;

import java.time.LocalDateTime;
import java.util.Objects;

/**
 * TODO (Copilot): fields customerId, fullName, email, phone, status, createdAt;
 * constructors, getters/setters; equals/hashCode on customerId only.
 * REJECT any @Entity / jakarta.persistence suggestions (plain Java Week 2).
 */
public class Customer {
    // TODO: implement fields and methods (use Copilot with a strong prompt)
}

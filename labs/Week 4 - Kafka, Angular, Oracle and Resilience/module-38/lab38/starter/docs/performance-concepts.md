# Lab 38 — Performance concepts

TODO: email lookup access path (UNIQUE vs `lower(email)`).

TODO: why `(status, created_at, customer_id)` helps the ACTIVE list.

TODO: N+1 (per-row account SELECT) vs one JOIN.

TODO: OFFSET vs keyset.

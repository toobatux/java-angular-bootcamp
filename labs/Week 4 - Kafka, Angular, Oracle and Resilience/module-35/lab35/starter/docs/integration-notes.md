# Lab 35 — Integration notes

Fill this after HttpClient works.

## Path

Browser (`:4200`) → Angular HttpClient → Spring Boot REST (`:8080`) → PostgreSQL (or this lab’s in-memory mock).

## DTO fields

List the JSON fields that match `Customer` in `customer.model.ts`.

## CORS

How did you allow `http://localhost:4200`?

## Correlation

Where is `X-Correlation-Id: lab-request-001` set?

# Lab 36 — Security notes

Fill after the guard and interceptor work.

## Token storage

Where does the access token live? Why is `localStorage` JWT not the graded default?

## Interceptor vs guard

What does the interceptor add? What does the guard **not** replace on the server?

## XSS

Does the login XSS probe stay text?

## CSRF

Cookie auth vs Bearer-from-memory — which CSRF controls apply?

## Headers

List CSP / `X-Content-Type-Options` / HTTPS for later deploy.

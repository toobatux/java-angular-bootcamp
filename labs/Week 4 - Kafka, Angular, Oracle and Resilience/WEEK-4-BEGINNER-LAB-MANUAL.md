# Week 4 Beginner Hands-On Lab Manual

## Objective

This manual turns Week 4 Labs 30 through 39 into a beginner-friendly training guide. It preserves the original technical scope while making every lab easier to follow independently.

Use this manual with the original lab guides. The original guides remain available for deeper detail, while this manual gives a consistent step-by-step path, expected outputs, checkpoints, common mistakes, and troubleshooting.

## Prerequisites

- Complete the course setup guide and Lab 0.
- Use your own working folder under `java-bootcamp/examples`.
- Do not edit the original course `starter` folders.
- Keep credentials in local environment variables or local `.env` files only.
- Do not commit `node_modules`, `dist`, `target`, database volumes, real `.env` files, passwords, tokens, or real personal data.

## Week 4 Learning Journey

| Lab | Topic | What it adds |
| --- | --- | --- |
| Lab 30 | Kafka event architecture | Topics, keys, partitions, offsets, groups |
| Lab 31 | Spring Kafka | `KafkaTemplate`, listeners, idempotency, DLT |
| Lab 32 | Resilience4j | Retry, circuit breaker, timeout, fallback |
| Lab 33 | Angular components | Standalone components, inputs/outputs, fixtures |
| Lab 34 | Angular state | Signals, events, reactive forms |
| Lab 35 | Angular and Spring API | HttpClient, errors, CORS, JSON REST |
| Lab 36 | Frontend security | tokens, route guards, XSS, CSRF/CSP notes |
| Lab 37 | PostgreSQL design | tables, keys, constraints, seeds |
| Lab 38 | PostgreSQL performance | `EXPLAIN`, indexes, sargability, paging |
| Lab 39 | Spring Data JPA | Flyway, entities, repositories, locking |

## General Beginner Workflow

1. Read the original guide.
2. Read the starter README.
3. Copy the starter into your examples folder.
4. Open the copied project in the correct tool.
5. Search for `TODO`.
6. Complete one TODO at a time.
7. Run the relevant command after each major change.
8. Capture expected output.
9. Record common mistakes and troubleshooting notes.

## PostgreSQL Dialect Note for Labs 37-39

Week 4 database labs are PostgreSQL labs. If an older guide or comment uses Oracle-style terms, use the PostgreSQL equivalent below:

| Older/Oracle-style wording | PostgreSQL equivalent for this manual |
| --- | --- |
| `NUMBER` for identifiers | `BIGINT` or `BIGINT GENERATED BY DEFAULT AS IDENTITY` |
| `DBMS_STATS` | `ANALYZE table_name;` |
| `DBMS_XPLAN` | `EXPLAIN (ANALYZE, BUFFERS)` |
| `TRUNC(created_at)` | `date_trunc(...)` for contrast, or a half-open timestamp range for optimized SQL |
| `DROP TABLE ... CASCADE CONSTRAINTS PURGE` | `DROP TABLE IF EXISTS table_name CASCADE;` |
| ORA error codes | PostgreSQL SQLSTATE codes such as `23505`, `23503`, and `23514` |

## Lab 30: Event-Driven Architecture with Kafka

### Lab Objective

Create Kafka topics and prove CRM customer events can be produced, consumed, ordered per customer key, and monitored for lag.

### Learning Outcomes

- Explain producer, consumer, topic, partition, offset, and consumer group in plain language.
- Start a local Kafka broker or connect to the instructor broker.
- Create `crm.customer-events.v1` and `crm.customer-events.v1.dlq`.
- Publish Amina and Ravi events keyed by `customerId`.
- Verify partition, offset, and lag output.

### Prerequisites

- Complete Lab 0 and the course setup guide.
- Read the original lab guide from start to finish before editing files.
- Original guide: [module-30/lab30/LAB-30-GUIDE.md](module-30/lab30/LAB-30-GUIDE.md)
- Starter instructions: [module-30/lab30/starter/README.md](module-30/lab30/starter/README.md)
- Confirm JDK 21 is available.
- Confirm Maven is available.
- Confirm Docker or instructor Kafka connection details is available.
- Confirm Terminal basics is available.

### Business Scenario

Northstar CRM needs asynchronous notifications and audit processing after customer changes. Kafka lets the CRM publish a fact once while multiple consumers process it independently.

Continuity:

- Previous work: Week 3 gave you secured and validated CRM APIs.
- New work: This lab adds Kafka topics, keyed customer events, producer and consumer verification, partitions, offsets, and consumer groups.
- Next handoff: Lab 31 reuses these topic names and event keys from Spring Boot.

### Exercise Overview

You copy the starter, start Kafka, create topics, publish JSON events, consume them with metadata, compare consumer groups, and document the handoff for Lab 31.

Important files and folders:

- `compose.yaml`
- `events/customer-created-amina.json`
- `events/customer-created-ravi.json`
- `src/main/java/com/northstar/crm/event/CustomerEventProducer.java`
- `docs/kafka-notes.md`

Screenshot placeholders:

- `[Screenshot: lab30-crm opened in the editor with the project tree visible]`
- `[Screenshot: terminal or browser output proving Lab 30 success]`

### Step-by-Step Instructions

#### Step 1: Copy the starter

Windows PowerShell:

```powershell
cd "<path-to-this-course>"
cd "labs\Week 4 - Kafka, Angular, Oracle and Resilience\module-30\lab30"
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\java-bootcamp\examples\lab30-crm" | Out-Null
Copy-Item -Recurse -Force ".\starter\*" "$env:USERPROFILE\java-bootcamp\examples\lab30-crm\"
cd "$env:USERPROFILE\java-bootcamp\examples\lab30-crm"
```

macOS or Linux terminal:

```bash
COURSE_REPO="<path-to-this-course>"
cd "$COURSE_REPO"
cd "labs/Week 4 - Kafka, Angular, Oracle and Resilience/module-30/lab30"
mkdir -p "$HOME/java-bootcamp/examples/lab30-crm"
cp -R starter/. "$HOME/java-bootcamp/examples/lab30-crm/"
cd "$HOME/java-bootcamp/examples/lab30-crm"
```

What these commands do:

- `cd` changes the active folder.
- `New-Item` or `mkdir -p` creates your personal lab folder.
- `Copy-Item` or `cp -R` copies starter files so you do not edit the course handout.
- The final `cd` moves into your copied project.

Common beginner mistakes:

- Editing the course `starter` folder instead of the copied examples folder.
- Running commands from the parent folder instead of the folder that contains `pom.xml`, `package.json`, `compose.yaml`, or the SQL scripts.
- Skipping the starter README.

Checkpoint:

- The copied project exists under `java-bootcamp/examples/lab30-crm`.
- You have opened the copied project in your editor.
- You have searched for `TODO` and read the original guide.

#### Step 2: Copy the starter

What to do: Use the common copy commands below. Open the copied folder, not the course handout folder.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 3: Start Kafka

What to do: Run `docker compose up -d` from the copied lab folder, or write down the shared bootstrap server your instructor provides.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 4: Create topics

What to do: Create `crm.customer-events.v1` with 3 partitions and `crm.customer-events.v1.dlq` with 1 partition.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 5: Write event JSON

What to do: Complete customer-created and status-changed sample files for `CUS-1001`, `CUS-1002`, and `lab-request-001`.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 6: Produce keyed messages

What to do: Use `customerId` as the key so all events for one customer stay in order within one partition.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 7: Consume with metadata

What to do: Print key, partition, offset, and timestamp so learners can verify the event lifecycle.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 8: Compare consumer groups

What to do: Run two consumers in `crm-notifications` and one in `crm-audit` to show competing vs independent consumption.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 9: Inspect lag

What to do: Stop a consumer, publish more events, run `kafka-consumer-groups.sh --describe`, then restart and watch lag decrease.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

### Code or Configuration

This event envelope is complete enough for the lab because it has an event id, type, version, customer id, correlation id, and business data.

```json
{
  "eventId": "e4b5f53d-7f18-4cf4-81ce-7cab6ec98491",
  "eventType": "CustomerCreated",
  "eventVersion": 1,
  "occurredAt": "2026-07-13T06:00:00Z",
  "customerId": "CUS-1001",
  "correlationId": "lab-request-001",
  "source": "customer-service",
  "data": {
    "fullName": "Amina Khan",
    "status": "ACTIVE"
  }
}
```

### Explanation

- Read the code from top to bottom before copying it.
- Confirm package names and import paths match the starter project.
- Replace only the matching TODO section unless the original guide asks for a full-file replacement.
- After adding code, run the relevant test or build command before continuing.

Important lines:

- The type or annotation tells the framework how to use the code.
- Configuration names must match the code exactly.
- Fixture values such as `CUS-1001`, `CUS-1002`, and `lab-request-001` keep labs connected.

### Expected Output

- Kafka broker is up.
- Both topics exist.
- Consumer output shows keys `CUS-1001` and `CUS-1002`.
- Lag can be described for `crm-notifications`.

### Commands and Verification

#### Command: Start Kafka

Run this from the copied `lab30-crm` project folder unless the step says otherwise.

```bash
docker compose up -d
```

What it does:

- Starts the local Kafka broker in the background.

Expected Output:

```text
Container is shown as running by `docker compose ps`.
```

How to verify success:

- The command returns to the prompt without an unexpected error.
- The output matches the expected message or status.
- If it fails, read the first error above the final failure line.

#### Command: Create the main topic

Run this from the copied `lab30-crm` project folder unless the step says otherwise.

```bash
docker exec crm-kafka /opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:9092 --create --topic crm.customer-events.v1 --partitions 3 --replication-factor 1
```

What it does:

- Creates the partitioned event topic.

Expected Output:

```text
Output says the topic was created, or describe shows PartitionCount: 3.
```

How to verify success:

- The command returns to the prompt without an unexpected error.
- The output matches the expected message or status.
- If it fails, read the first error above the final failure line.

#### Command: Describe the topic

Run this from the copied `lab30-crm` project folder unless the step says otherwise.

```bash
docker exec crm-kafka /opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:9092 --describe --topic crm.customer-events.v1
```

What it does:

- Shows partitions and leaders.

Expected Output:

```text
The topic name and three partitions appear.
```

How to verify success:

- The command returns to the prompt without an unexpected error.
- The output matches the expected message or status.
- If it fails, read the first error above the final failure line.

### Checkpoint

Before continuing, confirm that:

- The project is in your personal examples folder.
- The required TODOs for this lab are complete.
- The important command output has been captured.
- The required notes document has been updated.
- You can explain what changed and why.

### Common Mistakes

- Publishing without a key.
- Creating a topic with a misspelled name.
- Expecting global ordering across partitions.
- Putting secrets or real PII into event payloads.

### Troubleshooting

| Common error | Why it happens | How to fix it |
| --- | --- | --- |
| Consumer shows no records | Topic is wrong or no new records exist. | Use the exact topic name and produce a fresh message. |
| Producer times out | Broker is down or bootstrap server is wrong. | Run `docker compose ps` and verify `localhost:9092`. |
| Same customer appears on different partitions | The key was missing or parsed incorrectly. | Use `--property parse.key=true --property key.separator=:`. |

### Completion Checklist

- Lab 30 guide and starter README reviewed.
- All starter TODOs completed or documented.
- Commands run from the correct folder.
- Expected output captured.
- Notes document completed.
- At least one success path and one failure path verified.
- No secrets, generated folders, or real customer data committed.

### Lab Summary

In Lab 30, you practiced event-driven architecture with kafka for the Northstar CRM learning path. You should now be able to repeat the lab, explain the main concept in beginner language, show the files you changed, and prove the result with command or browser output.

## Lab 31: Spring Boot Integration with Kafka

### Lab Objective

Publish and consume versioned CRM events from a Spring Boot application safely and repeatably.

### Learning Outcomes

- Configure Spring Kafka.
- Publish typed events with `KafkaTemplate`.
- Consume events with `@KafkaListener`.
- Ignore duplicate event ids.
- Route invalid messages to a DLT or DLQ.

### Prerequisites

- Complete Lab 0 and the course setup guide.
- Read the original lab guide from start to finish before editing files.
- Original guide: [module-31/lab31/LAB-31-GUIDE.md](module-31/lab31/LAB-31-GUIDE.md)
- Starter instructions: [module-31/lab31/starter/README.md](module-31/lab31/starter/README.md)
- Confirm Lab 30 topics is available.
- Confirm JDK 21 is available.
- Confirm Maven is available.
- Confirm Spring Boot project basics is available.

### Business Scenario

When Amina or Ravi changes in CRM, Spring publishes an event. Notification and audit listeners must process each event once even if Kafka redelivers it.

Continuity:

- Previous work: Lab 30 created the Kafka broker, topic names, keys, offsets, and consumer group vocabulary.
- New work: This lab adds Spring Kafka with `KafkaTemplate`, `@KafkaListener`, JSON serialization, idempotency, retry, and dead-letter handling.
- Next handoff: Lab 32 adds resilience for outbound HTTP calls while preserving the event contracts.

### Exercise Overview

You configure Spring Kafka, create a customer event record, publish keyed events, listen with key validation, add idempotency, and test success plus poison-message behavior.

Important files and folders:

- `pom.xml`
- `src/main/resources/application.yml`
- `CustomerEvent.java`
- `CustomerEventPublisher.java`
- `CustomerEventListener.java`
- `ProcessedEventStore.java`
- `KafkaErrorConfig.java`
- `docs/spring-kafka-notes.md`

Screenshot placeholders:

- `[Screenshot: lab31-crm opened in the editor with the project tree visible]`
- `[Screenshot: terminal or browser output proving Lab 31 success]`

### Step-by-Step Instructions

#### Step 1: Copy the starter

Windows PowerShell:

```powershell
cd "<path-to-this-course>"
cd "labs\Week 4 - Kafka, Angular, Oracle and Resilience\module-31\lab31"
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\java-bootcamp\examples\lab31-crm" | Out-Null
Copy-Item -Recurse -Force ".\starter\*" "$env:USERPROFILE\java-bootcamp\examples\lab31-crm\"
cd "$env:USERPROFILE\java-bootcamp\examples\lab31-crm"
```

macOS or Linux terminal:

```bash
COURSE_REPO="<path-to-this-course>"
cd "$COURSE_REPO"
cd "labs/Week 4 - Kafka, Angular, Oracle and Resilience/module-31/lab31"
mkdir -p "$HOME/java-bootcamp/examples/lab31-crm"
cp -R starter/. "$HOME/java-bootcamp/examples/lab31-crm/"
cd "$HOME/java-bootcamp/examples/lab31-crm"
```

What these commands do:

- `cd` changes the active folder.
- `New-Item` or `mkdir -p` creates your personal lab folder.
- `Copy-Item` or `cp -R` copies starter files so you do not edit the course handout.
- The final `cd` moves into your copied project.

Common beginner mistakes:

- Editing the course `starter` folder instead of the copied examples folder.
- Running commands from the parent folder instead of the folder that contains `pom.xml`, `package.json`, `compose.yaml`, or the SQL scripts.
- Skipping the starter README.

Checkpoint:

- The copied project exists under `java-bootcamp/examples/lab31-crm`.
- You have opened the copied project in your editor.
- You have searched for `TODO` and read the original guide.

#### Step 2: Add dependencies

What to do: Open `pom.xml` and add `spring-kafka` plus `spring-kafka-test`.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 3: Configure Kafka

What to do: Open `application.yml` and set bootstrap servers, group id, trusted packages, and `crm.kafka.customer-topic`.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 4: Create the event type

What to do: Define `CustomerEvent` with event id, event type, version, customer id, correlation id, and data.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 5: Publish after successful CRM work

What to do: Use `KafkaTemplate.send(topic, customerId, event)` after create or status update succeeds.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 6: Listen and validate

What to do: Use `@KafkaListener` and compare the Kafka key to `event.customerId()`.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 7: Add idempotency

What to do: Store processed event ids before side effects so duplicates are ignored.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 8: Configure retry and DLT

What to do: Use `DefaultErrorHandler` and `DeadLetterPublishingRecoverer` for bounded retries.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 9: Run integration tests

What to do: Use EmbeddedKafka or Testcontainers and await a specific event id instead of sleeping blindly.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

### Code or Configuration

The listener validates the Kafka key before calling the handler. This prevents a malformed event from causing the wrong customer side effect.

```java
@KafkaListener(topics = "${crm.kafka.customer-topic}", groupId = "crm-notifications")
void receive(CustomerEvent event, @Header(KafkaHeaders.RECEIVED_KEY) String key) {
  if (!key.equals(event.customerId())) {
    throw new InvalidCustomerEventException("Kafka key must match customerId");
  }
  if (!processedEventStore.markIfNew(event.eventId())) {
    log.info("duplicate_event_ignored id={}", event.eventId());
    return;
  }
  notificationHandler.handle(event);
}
```

### Explanation

- Read the code from top to bottom before copying it.
- Confirm package names and import paths match the starter project.
- Replace only the matching TODO section unless the original guide asks for a full-file replacement.
- After adding code, run the relevant test or build command before continuing.

Important lines:

- The type or annotation tells the framework how to use the code.
- Configuration names must match the code exactly.
- Fixture values such as `CUS-1001`, `CUS-1002`, and `lab-request-001` keep labs connected.

### Expected Output

- A create/update publishes to `crm.customer-events.v1`.
- Listener logs one handling for the first event id.
- Replay logs `duplicate_event_ignored`.
- Bad key or bad version reaches the configured dead-letter destination.

### Commands and Verification

#### Command: Run tests

Run this from the copied `lab31-crm` project folder unless the step says otherwise.

```bash
mvn -B test
```

What it does:

- Compiles the Spring app and runs Kafka tests.

Expected Output:

```text
Expected output ends with `BUILD SUCCESS` after TODOs are complete.
```

How to verify success:

- The command returns to the prompt without an unexpected error.
- The output matches the expected message or status.
- If it fails, read the first error above the final failure line.

#### Command: Start app

Run this from the copied `lab31-crm` project folder unless the step says otherwise.

```bash
mvn spring-boot:run
```

What it does:

- Runs the Spring Boot app so it can publish and listen.

Expected Output:

```text
Logs show `Started CrmApplication`.
```

How to verify success:

- The command returns to the prompt without an unexpected error.
- The output matches the expected message or status.
- If it fails, read the first error above the final failure line.

### Checkpoint

Before continuing, confirm that:

- The project is in your personal examples folder.
- The required TODOs for this lab are complete.
- The important command output has been captured.
- The required notes document has been updated.
- You can explain what changed and why.

### Common Mistakes

- Leaving `spring.json.trusted.packages=*`.
- Marking events processed after the side effect.
- Using Thread.sleep as the only test synchronization.
- Forgetting to document whether DLT uses `.DLT` or `.dlq`.

### Troubleshooting

| Common error | Why it happens | How to fix it |
| --- | --- | --- |
| Listener silent | Offset already at end or wrong topic. | Publish a new event and verify `crm.kafka.customer-topic`. |
| Deserialization failure | Trusted package or type mapping is wrong. | Set trusted package to `com.northstar.crm.event` and align event class names. |
| Duplicate notifications | Idempotency check occurs after notification. | Call `markIfNew` before side effects. |

### Completion Checklist

- Lab 31 guide and starter README reviewed.
- All starter TODOs completed or documented.
- Commands run from the correct folder.
- Expected output captured.
- Notes document completed.
- At least one success path and one failure path verified.
- No secrets, generated folders, or real customer data committed.

### Lab Summary

In Lab 31, you practiced spring boot integration with kafka for the Northstar CRM learning path. You should now be able to repeat the lab, explain the main concept in beginner language, show the files you changed, and prove the result with command or browser output.

## Lab 32: Resilience4j for CRM Outbound Calls

### Lab Objective

Make CRM account summary reads survive slow or failing remote account services without lying to users.

### Learning Outcomes

- Configure Retry, CircuitBreaker, and TimeLimiter.
- Use WireMock to simulate 503 and slow responses.
- Return `AccountSummary.unavailable` for degraded reads.
- Observe circuit breaker state with Actuator.
- Explain why writes are not blindly retried.

### Prerequisites

- Complete Lab 0 and the course setup guide.
- Read the original lab guide from start to finish before editing files.
- Original guide: [module-32/lab32/LAB-32-GUIDE.md](module-32/lab32/LAB-32-GUIDE.md)
- Starter instructions: [module-32/lab32/starter/README.md](module-32/lab32/starter/README.md)
- Confirm JDK 21 is available.
- Confirm Maven is available.
- Confirm Spring Boot 3 is available.
- Confirm WireMock test awareness is available.

### Business Scenario

Agents should still see customer identity when the Account Profile service is slow. The UI should show accounts as temporarily unavailable instead of hanging or inventing balances.

Continuity:

- Previous work: Lab 31 handled asynchronous event reliability.
- New work: This lab protects synchronous outbound Account Profile calls with retry, circuit breaker, time limiter, and truthful fallback.
- Next handoff: Labs 33-36 build the Angular experience that must show degraded account data honestly.

### Exercise Overview

You add Resilience4j dependencies, configure an instance named `accountProfile`, simulate failures, add a fallback, and test OPEN, HALF_OPEN, timeout, and recovery behavior.

Important files and folders:

- `pom.xml`
- `src/main/resources/application.yml`
- `AccountProfileService.java`
- `AccountClient.java`
- `AccountSummary.java`
- `AccountProfileResilienceTest.java`
- `docs/resilience-notes.md`

Screenshot placeholders:

- `[Screenshot: lab32-crm opened in the editor with the project tree visible]`
- `[Screenshot: terminal or browser output proving Lab 32 success]`

### Step-by-Step Instructions

#### Step 1: Copy the starter

Windows PowerShell:

```powershell
cd "<path-to-this-course>"
cd "labs\Week 4 - Kafka, Angular, Oracle and Resilience\module-32\lab32"
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\java-bootcamp\examples\lab32-crm" | Out-Null
Copy-Item -Recurse -Force ".\starter\*" "$env:USERPROFILE\java-bootcamp\examples\lab32-crm\"
cd "$env:USERPROFILE\java-bootcamp\examples\lab32-crm"
```

macOS or Linux terminal:

```bash
COURSE_REPO="<path-to-this-course>"
cd "$COURSE_REPO"
cd "labs/Week 4 - Kafka, Angular, Oracle and Resilience/module-32/lab32"
mkdir -p "$HOME/java-bootcamp/examples/lab32-crm"
cp -R starter/. "$HOME/java-bootcamp/examples/lab32-crm/"
cd "$HOME/java-bootcamp/examples/lab32-crm"
```

What these commands do:

- `cd` changes the active folder.
- `New-Item` or `mkdir -p` creates your personal lab folder.
- `Copy-Item` or `cp -R` copies starter files so you do not edit the course handout.
- The final `cd` moves into your copied project.

Common beginner mistakes:

- Editing the course `starter` folder instead of the copied examples folder.
- Running commands from the parent folder instead of the folder that contains `pom.xml`, `package.json`, `compose.yaml`, or the SQL scripts.
- Skipping the starter README.

Checkpoint:

- The copied project exists under `java-bootcamp/examples/lab32-crm`.
- You have opened the copied project in your editor.
- You have searched for `TODO` and read the original guide.

#### Step 2: Add dependencies

What to do: Add Resilience4j Spring Boot 3, AOP, Actuator, and WireMock test dependencies.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 3: Configure retry

What to do: Use bounded attempts and backoff for safe GET calls only.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 4: Configure circuit breaker

What to do: Use a small count-based window for lab visibility.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 5: Configure time limiter

What to do: Set a 1500 ms timeout and return `CompletableFuture` from the protected method.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 6: Write fallback

What to do: Return `AccountSummary.unavailable(customerId)` and log only safe identifiers.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 7: Stub failures

What to do: Use WireMock for 503, slow 3000 ms, and recovery responses.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 8: Observe Actuator

What to do: Open health, metrics, and circuit breaker event endpoints.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

#### Step 9: Test deterministically

What to do: Assert WireMock call counts and circuit breaker states instead of relying on sleeps.

Why this matters:

- This step builds one part of the lab outcome before moving to the next dependency.

How to verify:

- Confirm the related file exists or the command output matches the expected result.
- If this step changes code, run the lab's test or build command after the change.

### Code or Configuration

The fallback must be truthful. It says account data is unavailable; it does not pretend an account read or write succeeded.

```java
@CircuitBreaker(name = "accountProfile", fallbackMethod = "fallback")
@Retry(name = "accountProfile")
@TimeLimiter(name = "accountProfile")
public CompletableFuture<AccountSummary> find(String customerId) {
  return CompletableFuture.supplyAsync(() -> accountClient.fetch(customerId));
}

private CompletableFuture<AccountSummary> fallback(String customerId, Throwable cause) {
  log.warn("account_profile_degraded customerId={} cause={}", customerId, cause.getClass().getSimpleName());
  return CompletableFuture.completedFuture(AccountSummary.unavailable(customerId));
}
```

### Explanation

- Read the code from top to bottom before copying it.
- Confirm package names and import paths match the starter project.
- Replace only the matching TODO section unless the original guide asks for a full-file replacement.
- After adding code, run the relevant test or build command before continuing.

Important lines:

- The type or annotation tells the framework how to use the code.
- Configuration names must match the code exactly.
- Fixture values such as `CUS-1001`, `CUS-1002`, and `lab-request-001` keep labs connected.

### Expected Output

- Healthy stub returns `available=true`.
- Slow stub falls back near 1500 ms.
- OPEN circuit fails fast without more WireMock calls.
- Fallback response has `available=false`.

### Commands and Verification

#### Command: Run resilience tests

Run this from the copied `lab32-crm` project folder unless the step says otherwise.

```bash
mvn -B test -Dtest=AccountProfileResilienceTest
```

What it does:

- Runs only the WireMock resilience test class.

Expected Output:

```text
Expected output ends with `BUILD SUCCESS`.
```

How to verify success:

- The command returns to the prompt without an unexpected error.
- The output matches the expected message or status.
- If it fails, read the first error above the final failure line.

#### Command: Check health

Run this from the copied `lab32-crm` project folder unless the step says otherwise.

```bash
curl -s http://localhost:8080/actuator/health
```

What it does:

- Reads Actuator health information.

Expected Output:

```text
Expected output is JSON containing status information.
```

How to verify success:

- The command returns to the prompt without an unexpected error.
- The output matches the expected message or status.
- If it fails, read the first error above the final failure line.

### Checkpoint

Before continuing, confirm that:

- The project is in your personal examples folder.
- The required TODOs for this lab are complete.
- The important command output has been captured.
- The required notes document has been updated.
- You can explain what changed and why.

### Common Mistakes

- Missing `spring-boot-starter-aop`.
- Using TimeLimiter on a synchronous return type.
- Retrying non-idempotent writes.
- Fallback returning fake success.

### Troubleshooting

| Common error | Why it happens | How to fix it |
| --- | --- | --- |
| Annotations ignored | AOP is missing or self-invocation bypasses Spring proxy. | Add AOP and call through an injected bean. |
| Circuit never opens | Instance name or failure exceptions do not match. | Use `accountProfile` consistently and assert recorded failures. |
| Fallback method not found | Signature does not match original args plus Throwable. | Use `fallback(String customerId, Throwable cause)`. |

### Completion Checklist

- Lab 32 guide and starter README reviewed.
- All starter TODOs completed or documented.
- Commands run from the correct folder.
- Expected output captured.
- Notes document completed.
- At least one success path and one failure path verified.
- No secrets, generated folders, or real customer data committed.

### Lab Summary

In Lab 32, you practiced resilience4j for crm outbound calls for the Northstar CRM learning path. You should now be able to repeat the lab, explain the main concept in beginner language, show the files you changed, and prove the result with command or browser output.

## Lab 33: Angular Component Architecture

### Lab Objective

Build a standalone Angular CRM UI with a smart list page and a presentational list item.

### Learning Outcomes

- Scaffold or copy an Angular 19 standalone app.
- Put CRM types and seeds in a feature folder.
- Use `input()` / `output()` between parent and child.
- Show Amina (`CUS-1001`) and Ravi (`CUS-1002`) in the browser.
- Write `docs/component-notes.md`.

### Prerequisites

- Complete Lab 0 and the course setup guide.
- Read the original lab guide from start to finish before editing files.
- Original guide: [module-33/lab33/LAB-33-GUIDE.md](module-33/lab33/LAB-33-GUIDE.md)
- Starter instructions: [module-33/lab33/starter/README.md](module-33/lab33/starter/README.md)
- Confirm Node.js 20+ (Node 24 is OK) is available.
- Confirm npm is available.
- Confirm Browser with DevTools is available.
- Confirm VS Code or IntelliJ is available.

### Business Scenario

The CRM needs a customer list that displays Amina and Ravi before it connects to the real API.

Continuity:

- Previous work: Backend labs have established CRM customers and status values.
- New work: This lab adds the first Angular standalone UI with smart vs presentational components.
- Next handoff: Lab 34 adds Signals and a filter form without rewriting the list item.

### Exercise Overview

You copy the Angular starter, complete the customer list TODOs, host the smart page in `AppComponent`, fill component notes, and run `npx ng serve` / `npx ng build`.

Important files and folders:

- `package.json`
- `src/app/features/customers/customer.model.ts`
- `src/app/features/customers/customer-list-item.component.ts`
- `src/app/features/customers/customer-list-page.component.ts`
- `src/app/app.component.ts`
- `docs/component-notes.md`

Screenshot placeholders:

- `[Screenshot: lab33-crm-ui opened in the editor with the project tree visible]`
- `[Screenshot: browser at http://localhost:4200 showing CUS-1001 and CUS-1002]`

### Step-by-Step Instructions

#### Step 1: Copy the starter

Windows PowerShell:

```powershell
cd "<path-to-this-course>"
cd "labs\Week 4 - Kafka, Angular, Oracle and Resilience\module-33\lab33"
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\java-bootcamp\examples\lab33-crm-ui" | Out-Null
Copy-Item -Recurse -Force ".\starter\*" "$env:USERPROFILE\java-bootcamp\examples\lab33-crm-ui\"
cd "$env:USERPROFILE\java-bootcamp\examples\lab33-crm-ui"
```

macOS or Linux terminal:

```bash
COURSE_REPO="<path-to-this-course>"
cd "$COURSE_REPO"
cd "labs/Week 4 - Kafka, Angular, Oracle and Resilience/module-33/lab33"
mkdir -p "$HOME/java-bootcamp/examples/lab33-crm-ui"
cp -R starter/. "$HOME/java-bootcamp/examples/lab33-crm-ui/"
cd "$HOME/java-bootcamp/examples/lab33-crm-ui"
```

Checkpoint:

- The copied project exists under `java-bootcamp/examples/lab33-crm-ui`.
- You have searched for `TODO` and read the original guide.

#### Step 2: Install packages

What to do: Run `npm install` in `lab33-crm-ui`.

#### Step 3: Feature model

What to do: Confirm `customer.model.ts` has `Customer` and `SEED_CUSTOMERS` for CUS-1001 / CUS-1002.

#### Step 4: Presentational list item

What to do: Bind `customer()` fields and emit `select` with the id on click. Do not hard-code Amina or Ravi in the child.

#### Step 5: Smart list page

What to do: Assign `SEED_CUSTOMERS`, `@for` the list items, and store `selectedId`.

#### Step 6: App shell

What to do: Host `<app-customer-list-page />` in `AppComponent`. Run `npx ng serve --open`.

#### Step 7: Notes and build

What to do: Fill `docs/component-notes.md`. Run `npx ng build`.

### Code or Configuration

The list item is presentational. It receives a customer through `input()` and tells the parent which id was selected.

```typescript
customer = input.required<Customer>();
select = output<string>();
```

### Expected Output

- Browser lists CUS-1001 Amina Khan and CUS-1002 Ravi Singh.
- Clicking a row updates Selected.
- `npx ng build` succeeds.
- Notes name smart vs presentational pieces.

### Commands and Verification

```bash
npm install
npx ng serve --open
npx ng build
```

Expected: `http://localhost:4200` shows both customers. `node_modules` and `dist` stay uncommitted.

### Common Mistakes

- Hard-coding names in the list item.
- Forgetting the child in the page `imports` array.
- Working only inside the course `labs/` clone.
- Committing `node_modules` or `dist`.

### Troubleshooting

| Common error | Why it happens | How to fix it |
| --- | --- | --- |
| Blank browser page | Missing standalone `imports` | Add `CustomerListPageComponent` / list item to `imports`. |
| CLI missing | No global `ng` | Use `npx ng`. |
| npm command missing | Wrong folder | Run commands inside `lab33-crm-ui`. |

### Completion Checklist

- Lab 33 guide and starter README reviewed.
- All starter TODOs completed or documented.
- Commands run from `examples/lab33-crm-ui`.
- Browser list and build evidence captured.
- `docs/component-notes.md` completed.
- No secrets, `node_modules`, or `dist` committed.

### Lab Summary

In Lab 33, you practiced Angular standalone components for the Northstar CRM UI. You should now be able to repeat the lab, explain smart vs presentational components, show the files you changed, and prove the result with `ng serve` or `ng build`.


## Lab 34: State and Event Management in Angular

### Lab Objective

Drive the CRM customer list with Signals, a computed filter, and a reactive status form.

### Learning Outcomes

- Hold `customers`, `loading`, and `error` in Signals.
- Derive a filtered list with `computed`.
- Wire a reactive status control to the filter Signal.
- Show loading, empty, and error branches.
- Write `docs/state-notes.md`.

### Prerequisites

- Complete Lab 0 and the course setup guide.
- Read the original lab guide from start to finish before editing files.
- Original guide: [module-34/lab34/LAB-34-GUIDE.md](module-34/lab34/LAB-34-GUIDE.md)
- Starter instructions: [module-34/lab34/starter/README.md](module-34/lab34/starter/README.md)
- Confirm Lab 33 Angular components is available (or use this lab’s starter).
- Confirm Node.js and npm are available.

### Business Scenario

List/filter state lives in Signals. Do not leave a forever spinner or blank screen when a filter matches nothing. Seeds stay Amina (`CUS-1001`) and Ravi (`CUS-1002`).

Continuity:

- Previous work: Lab 33 created a smart list page and presentational list item.
- New work: This lab adds Signals, a computed filter, a reactive form, and loading/empty/error UI.
- Next handoff: Lab 35 replaces seeds with HttpClient while keeping the same branches.

### Exercise Overview

You copy the Angular starter, complete TODOs in `CustomerListPageComponent`, fill state notes, and run `npx ng serve` / `npx ng build`.

Important files and folders:

- `src/app/features/customers/customer-list-page.component.ts`
- `src/app/features/customers/customer.model.ts`
- `docs/state-notes.md`

### Step-by-Step Instructions

#### Step 1: Copy the starter

Windows PowerShell:

```powershell
cd "<path-to-this-course>"
cd "labs\Week 4 - Kafka, Angular, Oracle and Resilience\module-34\lab34"
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\java-bootcamp\examples\lab34-crm-ui" | Out-Null
Copy-Item -Recurse -Force ".\starter\*" "$env:USERPROFILE\java-bootcamp\examples\lab34-crm-ui\"
cd "$env:USERPROFILE\java-bootcamp\examples\lab34-crm-ui"
npm install
```

macOS or Linux:

```bash
COURSE_REPO="<path-to-this-course>"
cd "$COURSE_REPO/labs/Week 4 - Kafka, Angular, Oracle and Resilience/module-34/lab34"
mkdir -p "$HOME/java-bootcamp/examples/lab34-crm-ui"
cp -R starter/. "$HOME/java-bootcamp/examples/lab34-crm-ui/"
cd "$HOME/java-bootcamp/examples/lab34-crm-ui"
npm install
```

#### Step 2: Signals and load

What to do: Implement `customers`, `loading`, `error`, and `loadSeeds()` with a short delay.

#### Step 3: Computed filter

What to do: `statusFilter` plus `filteredCustomers`. ACTIVE shows Amina; PROSPECT shows Ravi.

#### Step 4: Reactive form

What to do: Status `FormControl` `valueChanges` updates `statusFilter`.

#### Step 5: UI states

What to do: Template branches for loading, error, empty, and list. Use Simulate error for `lab-request-001 failed`.

#### Step 6: Notes and build

What to do: Fill `docs/state-notes.md`. Run `npx ng build`.

### Expected Output

- Filter ALL shows both customers; ACTIVE/PROSPECT isolate Amina/Ravi.
- Loading text appears briefly on start.
- Empty or error branch can be demonstrated.
- `npx ng build` succeeds.

### Commands and Verification

```bash
npm install
npx ng serve --open
npx ng build
```

### Common Mistakes

- Mutating `customers()` in place instead of `set([...])`.
- Binding the template to the raw list instead of `filteredCustomers()`.
- Forgetting `ReactiveFormsModule` in `imports`.
- Calling Signals “React hooks renamed.”

### Troubleshooting

| Common error | Why it happens | How to fix it |
| --- | --- | --- |
| Filter does nothing | Template still uses `customers()` | Bind `filteredCustomers()`. |
| Form ignored | Missing `ReactiveFormsModule` | Add it to `imports`. |
| Stale list | In-place array mutate | Replace the Signal value. |

### Lab Summary

In Lab 34, you practiced Angular Signals and reactive forms for the Northstar CRM list. You should now be able to filter Amina and Ravi, show loading/empty/error, and explain Signals vs Observables.


## Lab 35: Frontend to API Integration (Angular HttpClient)

### Lab Objective

Load Northstar customers from Spring Boot REST with a typed Angular HttpClient service, CORS, and the same loading/error branches from Lab 34.

### Learning Outcomes

- Register `provideHttpClient` and a correlation interceptor.
- Implement typed `getAll` / `getById` / `create` against `/api/customers`.
- Replace seed arrays with HTTP while keeping Signals for UI state.
- Configure `environment.apiBaseUrl` and CORS for `:4200` → `:8080`.
- Write `docs/integration-notes.md` (Angular → Boot → PostgreSQL).

### Prerequisites

- Complete Lab 0 and the course setup guide.
- Read the original lab guide from start to finish before editing files.
- Original guide: [module-35/lab35/LAB-35-GUIDE.md](module-35/lab35/LAB-35-GUIDE.md)
- Starter instructions: [module-35/lab35/starter/README.md](module-35/lab35/starter/README.md)
- Confirm Lab 34 Angular Signals is available (or use this lab’s starter).
- Confirm Node.js, npm, JDK 21, and Maven are available.

### Business Scenario

The browser must read Amina (`CUS-1001`) and Ravi (`CUS-1002`) from JSON REST. Angular never embeds SQL. Writes send `X-Correlation-Id: lab-request-001`. Persistence story is PostgreSQL (this lab uses an in-memory Boot stub).

Continuity:

- Previous work: Lab 34 held list state in Signals with a seed load.
- New work: This lab replaces seeds with HttpClient, env URLs, CORS, and a create for Maya (`CUS-1003`).
- Next handoff: Lab 36 adds auth interceptors and route guards on the same HTTP boundary.

### Exercise Overview

You copy the Angular starter and `crm-api`, start Boot on `:8080`, fill HttpClient TODOs, and prove the Network panel shows JSON for both customers.

Important files and folders:

- `src/environments/environment.ts`
- `src/app/app.config.ts`
- `src/app/core/correlation.interceptor.ts`
- `src/app/features/customers/customer-api.service.ts`
- `src/app/features/customers/customer-list-page.component.ts`
- `docs/integration-notes.md`
- `crm-api` (Spring Boot)

Screenshot placeholders:

- `[Screenshot: lab35-crm-ui opened in the editor with the project tree visible]`
- `[Screenshot: DevTools Network GET /api/customers showing CUS-1001 and CUS-1002]`

### Step-by-Step Instructions

#### Step 1: Copy the starter and API

Windows PowerShell:

```powershell
cd "<path-to-this-course>"
cd "labs\Week 4 - Kafka, Angular, Oracle and Resilience\module-35\lab35"
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\java-bootcamp\examples\lab35-crm-ui","$env:USERPROFILE\java-bootcamp\examples\lab35-crm-api" | Out-Null
Copy-Item -Recurse -Force ".\starter\*" "$env:USERPROFILE\java-bootcamp\examples\lab35-crm-ui\"
Copy-Item -Recurse -Force ".\crm-api\*" "$env:USERPROFILE\java-bootcamp\examples\lab35-crm-api\"
cd "$env:USERPROFILE\java-bootcamp\examples\lab35-crm-ui"
```

macOS or Linux terminal:

```bash
COURSE_REPO="<path-to-this-course>"
cd "$COURSE_REPO"
cd "labs/Week 4 - Kafka, Angular, Oracle and Resilience/module-35/lab35"
mkdir -p "$HOME/java-bootcamp/examples/lab35-crm-ui" "$HOME/java-bootcamp/examples/lab35-crm-api"
cp -R starter/. "$HOME/java-bootcamp/examples/lab35-crm-ui/"
cp -R crm-api/. "$HOME/java-bootcamp/examples/lab35-crm-api/"
cd "$HOME/java-bootcamp/examples/lab35-crm-ui"
```

Checkpoint:

- UI project exists under `java-bootcamp/examples/lab35-crm-ui`.
- API project exists under `java-bootcamp/examples/lab35-crm-api`.

#### Step 2: Start the Boot API

What to do: In a second terminal, `cd` into `lab35-crm-api` and run `mvn -B spring-boot:run`. Confirm `GET /api/customers/CUS-1001` returns Amina.

#### Step 3: Install UI packages

What to do: In `lab35-crm-ui`, run `npm install`.

#### Step 4: HttpClient + interceptor

What to do: Add `provideHttpClient(withInterceptors([correlationInterceptor]))`. Clone requests with header `X-Correlation-Id: lab-request-001`.

#### Step 5: Typed API service

What to do: Implement `CustomerApiService` using `environment.apiBaseUrl` and `/api/customers`.

#### Step 6: Replace seeds

What to do: Call `api.getAll()` from the list page. Keep loading / error / empty branches. Optional: POST Maya (`CUS-1003`).

#### Step 7: Notes and build

What to do: Fill `docs/integration-notes.md`. Run `npx ng serve --open` and `npx ng build`.

### Code or Configuration

HttpClient stays in the service. The presentational list item still only receives a `Customer` input.

```typescript
getAll(): Observable<Customer[]> {
  return this.http.get<Customer[]>(this.base);
}
```

### Expected Output

- Network GET `/api/customers` lists CUS-1001 and CUS-1002.
- Filter ACTIVE → Amina; PROSPECT → Ravi.
- Stop Boot → error branch (not a blank page).
- `npx ng build` succeeds.

### Commands and Verification

```bash
mvn -B -f examples/lab35-crm-api spring-boot:run
npm install
npx ng serve --open
npx ng build
```

### Common Mistakes

- Forgetting `provideHttpClient()` (NullInjectorError).
- Leaving `SEED_CUSTOMERS` in `loadCustomers()` so Network never fires.
- Wrong `apiBaseUrl` or missing CORS origin `http://localhost:4200`.
- Calling this a React/fetch lab.

### Troubleshooting

| Common error | Why it happens | How to fix it |
| --- | --- | --- |
| CORS error | Origin not allowed | Confirm `@CrossOrigin` on the Boot controller. |
| NullInjectorError: HttpClient | Provider missing | `provideHttpClient()` in `app.config.ts`. |
| Empty list, API 200 | DTO field names differ | Align `Customer` to JSON `id` / `name` / `email` / `status`. |
| Seeds still showing | TODO not done | Replace `SEED_CUSTOMERS` with `api.getAll()`. |

### Completion Checklist

- Lab 35 guide and starter README reviewed.
- All starter TODOs completed or documented.
- Commands run from `examples/lab35-crm-ui` with Boot on `:8080`.
- Network JSON and build evidence captured.
- `docs/integration-notes.md` completed.
- No secrets, `node_modules`, or `dist` committed.

### Lab Summary

In Lab 35, you practiced Angular HttpClient against the Northstar Spring Boot REST API. You should now be able to load Amina and Ravi from JSON, explain CORS and env base URLs, and keep loading/error UI when the API is down.

## Lab 36: Secure Frontend Communication (Angular)

### Lab Objective

Protect the CRM UI with a memory-only access token, an auth interceptor, a route guard, and security notes. Do not treat localStorage JWT as the recommended default.

### Learning Outcomes

- Hold the demo Bearer token in a Signal (memory).
- Attach `Authorization` only to the CRM API origin.
- Guard `/customers` and keep `/login` public.
- Logout on HTTP 401; keep the session on 403.
- Render an XSS probe as text; document CSRF vs cookies.

### Prerequisites

- Complete Lab 0 and the course setup guide.
- Read the original lab guide from start to finish before editing files.
- Original guide: [module-36/lab36/LAB-36-GUIDE.md](module-36/lab36/LAB-36-GUIDE.md)
- Starter instructions: [module-36/lab36/starter/README.md](module-36/lab36/starter/README.md)
- Confirm Lab 35 HttpClient is available (or use this lab’s starter).
- Confirm Node.js, npm, JDK 21, and Maven are available.

### Business Scenario

Customer APIs are authenticated. Guards keep anonymous users off `/customers`. Spring still returns 401 without `Authorization: Bearer lab-demo-token`. Seeds Amina (`CUS-1001`) and Ravi (`CUS-1002`) stay behind that check.

Continuity:

- Previous work: Lab 35 loaded the list with HttpClient.
- New work: This lab adds memory session, interceptor, guard, XSS/CSRF notes, and a Boot demo Bearer filter.
- Next handoff: Lab 37 designs PostgreSQL storage; browser security remains required.

### Exercise Overview

You copy the Angular starter and `crm-api`, start Boot, fill auth TODOs, and prove redirect → mock login → Network `Authorization` → Amina/Ravi.

Important files and folders:

- `src/app/app.routes.ts`
- `src/app/core/auth/auth-session.service.ts`
- `src/app/core/auth/auth.interceptor.ts`
- `src/app/core/auth/auth.guard.ts`
- `src/app/features/login/login-page.component.ts`
- `docs/security-notes.md`
- `crm-api` (`DemoBearerFilter`)

Screenshot placeholders:

- `[Screenshot: cold /customers redirected to /login]`
- `[Screenshot: DevTools Network GET /api/customers with Authorization (redact token if sharing)]`

### Step-by-Step Instructions

#### Step 1: Copy the starter and API

Windows PowerShell:

```powershell
cd "<path-to-this-course>"
cd "labs\Week 4 - Kafka, Angular, Oracle and Resilience\module-36\lab36"
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\java-bootcamp\examples\lab36-crm-ui","$env:USERPROFILE\java-bootcamp\examples\lab36-crm-api" | Out-Null
Copy-Item -Recurse -Force ".\starter\*" "$env:USERPROFILE\java-bootcamp\examples\lab36-crm-ui\"
Copy-Item -Recurse -Force ".\crm-api\*" "$env:USERPROFILE\java-bootcamp\examples\lab36-crm-api\"
cd "$env:USERPROFILE\java-bootcamp\examples\lab36-crm-ui"
```

macOS or Linux terminal:

```bash
COURSE_REPO="<path-to-this-course>"
cd "$COURSE_REPO"
cd "labs/Week 4 - Kafka, Angular, Oracle and Resilience/module-36/lab36"
mkdir -p "$HOME/java-bootcamp/examples/lab36-crm-ui" "$HOME/java-bootcamp/examples/lab36-crm-api"
cp -R starter/. "$HOME/java-bootcamp/examples/lab36-crm-ui/"
cp -R crm-api/. "$HOME/java-bootcamp/examples/lab36-crm-api/"
cd "$HOME/java-bootcamp/examples/lab36-crm-ui"
```

#### Step 2: Start the Boot API

What to do: `mvn -B spring-boot:run` in `lab36-crm-api`. Confirm GET `/api/customers` is 401 without Bearer, and 200 with `Authorization: Bearer lab-demo-token`.

#### Step 3: Memory session

What to do: `loginMock()` sets `lab-demo-token` on `accessToken`. Do not write it to `localStorage`.

#### Step 4: Interceptor and 401

What to do: Clone requests to the API origin with `Authorization`. On 401, logout and go to `/login`. Do not logout on 403.

#### Step 5: Guard and login

What to do: `authGuard` sends anonymous users to `/login`. Mock login then navigates to `/customers`. Confirm the XSS probe stays text.

#### Step 6: Notes and build

What to do: Fill `docs/security-notes.md`. Run `npx ng serve --open` and `npx ng build`.

### Code or Configuration

Token lives in a Signal. The interceptor reads `accessToken()` — it does not call `localStorage`.

```typescript
export const authInterceptor: HttpInterceptorFn = (req, next) => {
  const token = inject(AuthSessionService).accessToken();
  if (!token) return next(req);
  return next(req.clone({ setHeaders: { Authorization: `Bearer ${token}` } }));
};
```

### Expected Output

- `/customers` while logged out → `/login`.
- After mock login, Network shows `Authorization` and the list includes CUS-1001 and CUS-1002.
- XSS probe is visible as characters, not an image.
- `npx ng build` succeeds.

### Commands and Verification

```bash
mvn -B -f examples/lab36-crm-api spring-boot:run
npm install
npx ng serve --open
npx ng build
```

### Common Mistakes

- Saving the JWT in `localStorage` “so refresh works.”
- Attaching Bearer to every URL, not just the API origin.
- Treating the route guard as backend authorization.
- Committing a real token.

### Troubleshooting

| Common error | Why it happens | How to fix it |
| --- | --- | --- |
| Always login page | Token never set | Complete `loginMock()`. |
| 401 after login | Interceptor not registered | Add `authInterceptor` to `withInterceptors`. |
| List without login | Guard returns true | Redirect when `accessToken()` is null. |
| CORS + Authorization | Preflight blocked | Boot must skip OPTIONS in the Bearer filter. |

### Completion Checklist

- Lab 36 guide and starter README reviewed.
- All starter TODOs completed or documented.
- Commands run from `examples/lab36-crm-ui` with Boot on `:8080`.
- Redirect and Authorization evidence captured (token redacted).
- `docs/security-notes.md` completed.
- No secrets, `node_modules`, or `dist` committed.

### Lab Summary

In Lab 36, you practiced memory-token auth, interceptors, and route guards for the Northstar CRM UI. You should now be able to explain why localStorage JWT is not the graded default, show a guarded `/customers` route, and prove Boot still returns 401 without Bearer.

## Lab 37: PostgreSQL Design for Customers and Accounts

### Lab Objective

Create a PostgreSQL CRM schema with named constraints, least-privilege `crm_app`, Amina/Ravi seeds, and negative checks. PostgreSQL only — not Oracle.

### Learning Outcomes

- Separate surrogate `customer_id` from API `public_id` (`CUS-1001`).
- Model Customer 1:N Account with FK, UNIQUE, and CHECK.
- Store money as `balance_cents BIGINT` on the timed path.
- Seed Amina ACTIVE + account and Ravi PROSPECT.
- Prove duplicate email, illegal status, and orphan FK fail.

### Prerequisites

- Complete Lab 0 and the course setup guide.
- Read the original lab guide from start to finish before editing files.
- Original guide: [module-37/lab37/LAB-37-GUIDE.md](module-37/lab37/LAB-37-GUIDE.md)
- Starter instructions: [module-37/lab37/starter/README.md](module-37/lab37/starter/README.md)
- Confirm Docker Desktop (or shared PostgreSQL) is available.

### Business Scenario

Angular talks to Spring REST; Spring will persist here in Lab 39. Integrity belongs in PostgreSQL, not only in Java or the UI.

Continuity:

- Previous work: Labs 33–36 built and secured the Angular → REST flow.
- New work: This lab freezes `customer` + `account` with constraints and seeds.
- Next handoff: Lab 38 tunes SQL on this schema; Lab 39 maps it with JPA/Flyway.

### Exercise Overview

You copy the starter, start Postgres, fill SQL TODOs, seed CUS-1001 / CUS-1002, and run verify (including negatives).

Important files and folders:

- `compose.yaml`
- `database/01_create_user.sql`
- `database/02_schema.sql`
- `database/03_seed.sql`
- `database/04_verify.sql`
- `database/05_drop.sql`
- `database/design-decisions.md`
- `docs/postgres-notes.md`

Screenshot placeholders:

- `[Screenshot: lab37-crm opened in the editor with the project tree visible]`
- `[Screenshot: psql SELECT showing CUS-1001 Amina and CUS-1002 Ravi]`

### Step-by-Step Instructions

#### Step 1: Copy the starter

Windows PowerShell:

```powershell
cd "<path-to-this-course>"
cd "labs\Week 4 - Kafka, Angular, Oracle and Resilience\module-37\lab37"
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\java-bootcamp\examples\lab37-crm" | Out-Null
Copy-Item -Recurse -Force ".\starter\*" "$env:USERPROFILE\java-bootcamp\examples\lab37-crm\"
cd "$env:USERPROFILE\java-bootcamp\examples\lab37-crm"
Copy-Item .env.example .env
```

macOS or Linux terminal:

```bash
COURSE_REPO="<path-to-this-course>"
cd "$COURSE_REPO/labs/Week 4 - Kafka, Angular, Oracle and Resilience/module-37/lab37"
mkdir -p "$HOME/java-bootcamp/examples/lab37-crm"
cp -R starter/. "$HOME/java-bootcamp/examples/lab37-crm/"
cd "$HOME/java-bootcamp/examples/lab37-crm"
cp .env.example .env
```

#### Step 2: Start PostgreSQL

What to do: `docker compose up -d` then wait until `pg_isready` succeeds.

#### Step 3: Role and DDL as `crm`

What to do: Fill `01_create_user.sql` (`crm_app` LOGIN, not SUPERUSER). Fill `02_schema.sql` constraints, indexes, and GRANTs. Apply **01** and **02** as user `crm`.

#### Step 4: Seed as `crm_app`

What to do: Insert Amina (`CUS-1001`, ACTIVE) plus one account, and Ravi (`CUS-1002`, PROSPECT) with no account. Apply `03_seed.sql` as `crm_app`.

#### Step 5: Verify and notes

What to do: Complete `04_verify.sql` negatives (duplicate email, `SUSPENDED` status, orphan FK). Fill design + ACID notes. Document `jdbc:postgresql://localhost:5432/crm`.

### Code or Configuration

Timed-path money is integer cents. Status CHECK does not allow `SUSPENDED`.

```sql
CONSTRAINT ck_customer_status
  CHECK (status IN ('PROSPECT', 'ACTIVE', 'CLOSED'))
```

### Expected Output

- Two customer rows: CUS-1001 ACTIVE, CUS-1002 PROSPECT.
- One account for Amina (`ACC-1001-01`).
- Duplicate email → SQLSTATE 23505.
- Illegal status → SQLSTATE 23514.
- Orphan account → SQLSTATE 23503.
- `crm_app` is not SUPERUSER.

### Commands and Verification

```bash
docker compose up -d
# 01 + 02 as crm; 03 + 04 as crm_app (see OS how-to for PowerShell pipes)
```

### Common Mistakes

- Applying DDL as `crm_app` before GRANTs (permission denied).
- Using Oracle types (`NUMBER`, `VARCHAR2`) in this lab.
- Storing money only as `NUMERIC` on the timed path (Lab contract is `BIGINT` cents).
- Committing `.env`.

### Troubleshooting

| Common error | Why it happens | How to fix it |
| --- | --- | --- |
| Connection refused | Compose not up / port 5432 busy | `docker compose ps`; stop other Postgres. |
| Permission denied for table | Grants missing | GRANT after DDL; seed as `crm_app`. |
| FK violation on seed | Account before customer | Insert CUS-1001 first. |
| Table already exists | Re-run without drop | Run `05_drop.sql` then schema/seed again. |

### Completion Checklist

- Lab 37 guide and starter README reviewed.
- All starter TODOs completed or documented.
- Commands run from `examples/lab37-crm`.
- Seed SELECT and negative-check evidence captured.
- Passwords only in `.env`.
- No secrets, `target`, or Docker volumes committed.

### Lab Summary

In Lab 37, you practiced PostgreSQL DDL for the Northstar CRM. You should now be able to explain `public_id` vs `customer_id`, show Amina and Ravi in `psql`, and prove UNIQUE/CHECK/FK failures.

## Lab 38: PostgreSQL SQL and CRM Query Performance

### Lab Objective

Measure PostgreSQL query plans with `EXPLAIN (ANALYZE, BUFFERS)`, add indexes only after a baseline, and compare OFFSET vs keyset paging. Not Oracle `DBMS_XPLAN`.

### Learning Outcomes

- Generate volume while keeping CUS-1001 / CUS-1002.
- Capture baseline plans before `CREATE INDEX`.
- Add a list index on `(status, created_at, customer_id)` and re-measure.
- Rewrite `date_trunc` filters as half-open ranges.
- Document JOIN/CTE/aggregation and N+1 vs one JOIN.

### Prerequisites

- Complete Lab 0 and the course setup guide.
- Read the original lab guide from start to finish before editing files.
- Original guide: [module-38/lab38/LAB-38-GUIDE.md](module-38/lab38/LAB-38-GUIDE.md)
- Starter instructions: [module-38/lab38/starter/README.md](module-38/lab38/starter/README.md)
- Confirm Docker Desktop (or Lab 37 Postgres still running).

### Business Scenario

Guessing indexes can slow writes. Every retained index must name the CRM query and show before/after plans.

Continuity:

- Previous work: Lab 37 created `customer` / `account` and seeded Amina/Ravi.
- New work: This lab loads volume, measures plans, and justifies indexes.
- Next handoff: Lab 39 maps this schema with JPA/Flyway without inventing indexes.

### Exercise Overview

You copy the starter (includes Lab 37 DDL), start Postgres, fill performance TODOs, and write `report.md`.

Important files and folders:

- `database/ddl/`
- `database/performance/01_generate_data.sql` … `05_cleanup_indexes.sql`
- `database/performance/report.md`
- `docs/performance-concepts.md`

Screenshot placeholders:

- `[Screenshot: lab38-crm project tree]`
- `[Screenshot: EXPLAIN ANALYZE for ACTIVE list before and after the composite index]`

### Step-by-Step Instructions

#### Step 1: Copy the starter

Windows PowerShell:

```powershell
cd "<path-to-this-course>"
cd "labs\Week 4 - Kafka, Angular, Oracle and Resilience\module-38\lab38"
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\java-bootcamp\examples\lab38-crm" | Out-Null
Copy-Item -Recurse -Force ".\starter\*" "$env:USERPROFILE\java-bootcamp\examples\lab38-crm\"
cd "$env:USERPROFILE\java-bootcamp\examples\lab38-crm"
Copy-Item .env.example .env
```

#### Step 2: Start Postgres and apply DDL

What to do: `docker compose up -d`. Apply `ddl/01` → `02` → `03` as user `crm`. Confirm CUS-1001 / CUS-1002.

#### Step 3: Generate volume

What to do: `generate_series` ≥ tens of thousands. Do not overwrite Amina/Ravi. `ANALYZE`.

#### Step 4: Baseline then indexes

What to do: Run `02_baseline.sql` **before** `03_indexes.sql`. Paste scan type, buffers, and time into `report.md` as lab38-001+.

#### Step 5: Optimized SQL and paging

What to do: Range filter vs `date_trunc`; OFFSET vs keyset; JOIN/LEFT JOIN; CTE counts. Fill concepts notes.

### Code or Configuration

```sql
EXPLAIN (ANALYZE, BUFFERS)
SELECT public_id, full_name, created_at, customer_id
FROM customer
WHERE status = 'ACTIVE'
ORDER BY created_at, customer_id
LIMIT 50;
```

### Expected Output

- `count(*)` on `customer` includes 50k load rows plus two fixtures.
- Exact email lookup uses the UNIQUE index.
- ACTIVE list improves after `(status, created_at, customer_id)`.
- `report.md` has lab38-001 through lab38-004.

### Commands and Verification

See [LAB-38-WINDOWS.md](module-38/lab38/LAB-38-WINDOWS.md) for PowerShell pipes (no POSIX `<`).

### Common Mistakes

- Creating indexes before capturing a baseline.
- Using `email_normalized` — Lab 37 column is `email`.
- `DBMS_XPLAN` / Oracle hints.
- Deleting CUS-1001 while loading data.

### Troubleshooting

| Common error | Why it happens | How to fix it |
| --- | --- | --- |
| Index ignored | `lower(email)` or stale stats | Sargable predicate; `ANALYZE`. |
| Tiny-table plans | Forgot generate script | Run `01_generate_data.sql`. |
| Unique violation | Generated `public_id` collided | Use `CUS-L` prefix + `ON CONFLICT`. |
| Fixtures missing | Load overwrote seeds | Re-run `ddl/03_seed.sql`. |

### Completion Checklist

- Lab 38 guide and starter README reviewed.
- Baseline captured before indexes.
- report.md completed with experiment ids.
- No secrets or 50k-row dumps committed.

### Lab Summary

In Lab 38, you practiced evidence-based PostgreSQL tuning for Northstar CRM. You should now be able to read `EXPLAIN (ANALYZE, BUFFERS)`, justify a list index, and explain why keyset paging beats a deep OFFSET.

## Lab 39: Spring Data JPA with PostgreSQL

### Lab Objective

Map the Lab 37/38 PostgreSQL CRM schema with Flyway and Spring Data JPA. Graded IT runs on real PostgreSQL — not H2.

### Learning Outcomes

- Configure `jdbc:postgresql://localhost:5432/crm` with env vars.
- Own DDL with Flyway; keep `ddl-auto=validate`.
- Map `CustomerEntity` / `AccountEntity` (`email`, `balance_cents`, `@Version`).
- Use `findByPublicId`, paged `findByStatus`, and bounded page size.
- Map duplicate/optimistic conflicts to HTTP 409 without leaking SQL.

### Prerequisites

- Complete Lab 0 and the course setup guide.
- Original guide: [module-39/lab39/LAB-39-GUIDE.md](module-39/lab39/LAB-39-GUIDE.md)
- Starter: [module-39/lab39/starter/README.md](module-39/lab39/starter/README.md)
- JDK 21, Maven, Docker Desktop.

### Business Scenario

Angular still talks REST. Persistence is PostgreSQL. Schema is not Hibernate `update`.

Continuity:

- Previous work: Labs 37–38 designed and measured the tables.
- New work: This lab maps those columns with Flyway + JPA and proves it with a Postgres IT.
- Next handoff: Week 5 AppSec/CI can assume this verify-green baseline.

### Exercise Overview

Copy the starter, start Compose Postgres, fill TODOs, run `mvn -B test`.

Important files:

- `pom.xml`
- `application.yml`
- `db/migration/V1__crm_schema.sql`
- `CustomerEntity.java` / `AccountEntity.java`
- `CustomerRepository.java` / `CustomerService.java`
- `CustomerRepositoryIT.java`
- `docs/jpa-postgres-notes.md`

### Step-by-Step Instructions

#### Step 1: Copy the starter

Windows PowerShell:

```powershell
cd "<path-to-this-course>\labs\Week 4 - Kafka, Angular, Oracle and Resilience\module-39\lab39"
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\java-bootcamp\examples\lab39-crm" | Out-Null
Copy-Item -Recurse -Force ".\starter\*" "$env:USERPROFILE\java-bootcamp\examples\lab39-crm\"
cd "$env:USERPROFILE\java-bootcamp\examples\lab39-crm"
Copy-Item .env.example .env
```

#### Step 2: Start PostgreSQL

What to do: `docker compose up -d` and wait until healthy.

#### Step 3: Flyway V1

What to do: Align V1 with Lab 37 (`email`, not `email_normalized`; `balance_cents BIGINT`). Add Lab 38 indexes if you keep them.

#### Step 4: Entities, repos, service

What to do: `@Version`; LAZY relationship; implement `create`; bound page `size` to 50.

#### Step 5: IT on PostgreSQL

What to do: Point `CustomerRepositoryIT` at Compose (or Testcontainers). Persist/find CUS-1001. Duplicate email must fail. No H2 dependency.

### Code or Configuration

```yaml
spring.jpa.hibernate.ddl-auto: validate
spring.jpa.open-in-view: false
```

### Expected Output

- Flyway applies V1; `CUS-1001` Amina is findable.
- `mvn -B test` green twice against PostgreSQL.
- Duplicate email → 409, not a SQLException body.

### Commands and Verification

```bash
docker compose up -d
mvn -B test
mvn -B test
```

### Common Mistakes

- Mapping `email_normalized` (Lab 37 column is `email`).
- `ddl-auto=update` in Git.
- IT on H2 only.
- Returning a LAZY `accounts` collection with `open-in-view=false`.

### Troubleshooting

| Common error | Why it happens | How to fix it |
| --- | --- | --- |
| Relation does not exist | Flyway did not run | Check URL/DB and `db/migration`. |
| Validation failed | Entity ≠ V1 | Align names/types (`balance_cents`). |
| Connection refused | Compose not up | `docker compose ps`. |
| LazyInitializationException | Serialize LAZY graph | `@JsonIgnore` or fetch in the service TX. |

### Completion Checklist

- Lab 39 guide and starter README reviewed.
- `mvn -B test` green on PostgreSQL.
- Notes explain Flyway vs `update`.
- No `.env` or `target/` committed.

### Lab Summary

In Lab 39, you practiced Spring Data JPA against PostgreSQL for Northstar CRM. You should now be able to explain why Flyway owns schema, find Amina by `CUS-1001`, and prove tests did not run on H2.
